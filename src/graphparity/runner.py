"""
Entry point placeholder.

Wired up as project.scripts.graphparity so `uv run graphparity` works
from day one, even before the real orchestration logic (warm-up +
timed runs across all 5 adapters) is written.
"""


def main() -> None:
    print("GraphParity scaffold is up. Nothing to benchmark yet.")


if __name__ == "__main__":
    main()
